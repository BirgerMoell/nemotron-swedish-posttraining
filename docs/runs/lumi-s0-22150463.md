# LUMI S0 result — job 22150463

**Outcome: PASS** on 2026-09-18. The job completed on one AMD Instinct MI250X GCD in 1m33s with exit code 0. The artifact validator reported no failures.

## Evidence

| Field | Result |
|---|---|
| Repository commit | `fc93f81c9671e8d3bd9014f6f452183e4b7fbb55` |
| Model revision | `dfaf35de3e30f1867dd8dbc38a7fc9fb52d3914f` |
| Dataset revision | `e89e3c9f1385f2656fa0a5bed97b45193cfadcbf` |
| Input JSONL SHA-256 | `5c7b9ef1f919fdfef1f25738dc3981fb88200f48269817965ef2e4eda6ab838d` |
| Losses | `8.906970`, `8.670029` |
| Gradient norms | `6.465034`, `6.929615` |
| Tokens per step | 256 total; 234 supervised; 22 masked prompt |
| Trainable / total parameters | 5,059,584 / 3,978,616,416 |
| Peak GPU allocation | 19,119,140,352 bytes (17.81 GiB) |
| Adapter | 20 MB, SHA-256 `cc7968ac4624f709a63bf9eaaf8ddfbae24be5c8ee82baeeb36cc820528ea3c7` |

Runtime: Python 3.12.3, PyTorch 2.9.1+ROCm 6.4, Transformers 4.57.3 and PEFT 0.18.1. The model used eager attention and the pure-PyTorch Mamba fallback. The exact-source RMSNorm compatibility patch produced model-code SHA-256 `097d41e709bd685b0bbc178a8751f04a8ab6d5f62310b9b9d9b66879ed66410a`.

## Iteration history

- `22150231` failed before model load because NVIDIA's remote code imported `mamba_ssm` gated RMSNorm unconditionally. The repo now applies an exact-SHA-guarded PyTorch equivalent during staging.
- `22150379` completed, but both 256-token windows contained only the tail of long assistant answers. This proved gradient flow but did not exercise a prompt/assistant mask boundary.
- `22150463` added a fail-closed minimum of 16 masked prompt tokens and passed with 22 on both steps.

## Interpretation and limitations

S0 qualifies model loading, the ROCm fallback, assistant-only loss masking, LoRA updates, adapter serialization and manifests. It does not measure a Swedish capability change: the 4B checkpoint is already post-trained, only two updates ran, and the masking gate selected the same qualifying short conversation for both steps. S1 must stratify for shorter examples, require distinct row IDs, add pre/post Swedish generation probes and run enough updates for a real loss curve.

The adapter and complete execution directory remain on LUMI at:

`/scratch/project_465002530/users/bmoell/nemotron-swedish-posttraining/runs/22150463`

